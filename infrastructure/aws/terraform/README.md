command

##download init plugins
terraform init

##exec the script
terraform apply

##exec with parameters
terraform apply -var name="vpc_name"

##delete all resourse
terraform apply -var name="vpc_name"
