version: 0.0
os: linux

files:
  - source: webapp
    destination: /var/webapps
  - source: app.jar
    destination: /opt/tomcat/webapps

hooks:
  AfterInstall:
   - location: ./afterInstall.sh
     timeout: 180
     runas: centos
